# 屿物 - 文创商品购物网站

## 项目概述

"屿物"是一个文创商品购物网站，采用 Angular + Java Spring Boot + MySQL 技术栈开发。

### 技术栈
- **前端**: Angular 18.x + TypeScript
- **后端**: Spring Boot 3.2 + MyBatis-Plus
- **数据库**: MySQL 8.0+
- **认证**: JWT (JSON Web Token)
- **UI风格**: 温暖简洁文艺风格

### 项目结构
```
WenChuangShop/
├── backend/                    # 后端项目
│   ├── pom.xml                # Maven配置
│   └── src/main/
│       ├── java/com/yuwen/wenchuang/
│       │   ├── WenChuangShopApplication.java    # 启动类
│       │   ├── common/          # 通用类
│       │   ├── config/          # 配置类
│       │   ├── controller/      # 控制器
│       │   ├── dto/             # 数据传输对象
│       │   ├── entity/          # 实体类
│       │   ├── guards/          # 路由守卫
│       │   ├── interceptors/    # 拦截器
│       │   ├── mapper/          # Mapper接口
│       │   ├── models/          # 模型类
│       │   ├── security/        # 安全配置
│       │   └── service/         # 服务层
│       └── resources/
│           ├── application.yml   # 应用配置
│           ├── schema.sql        # 数据库表结构
│           └── data.sql          # 初始数据
│
└── frontend/                   # 前端项目
    ├── package.json
    ├── angular.json
    ├── tsconfig.json
    └── src/
        ├── app/
        │   ├── components/      # 组件
        │   ├── guards/          # 路由守卫
        │   ├── interceptors/    # HTTP拦截器
        │   ├── models/          # 类型定义
        │   ├── services/        # 服务
        │   ├── app.component.ts
        │   ├── app.module.ts
        │   └── app-routing.module.ts
        ├── styles.css           # 全局样式
        ├── index.html
        └── main.ts
```

---

## 环境准备

### 必需环境
1. **JDK 17+**
2. **Node.js 18+**
3. **MySQL 8.0+**
4. **Maven 3.8+**
5. **Angular CLI 18+** (可选，npm scripts已配置)

### 环境检查
```bash
# 检查Java版本
java -version

# 检查Node.js版本
node -v

# 检查MySQL版本
mysql --version
```

---

## 数据库配置

### 1. 创建数据库
```sql
-- 登录MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE wenchuang_shop DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. 导入数据库结构和数据
```bash
# 方式一：使用MySQL命令行
mysql -u root -p wenchuang_shop < backend/src/main/resources/schema.sql
mysql -u root -p wenchuang_shop < backend/src/main/resources/data.sql

# 方式二：在MySQL中手动执行
source /path/to/WenChuangShop/backend/src/main/resources/schema.sql
source /path/to/WenChuangShop/backend/src/main/resources/data.sql
```

### 3. 修改数据库连接配置
编辑 `backend/src/main/resources/application.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/wenchuang_shop?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root      # 修改为你的MySQL用户名
    password: root      # 修改为你的MySQL密码
```

---

## 启动后端服务

### 方式一：使用Maven命令
```bash
# 进入后端目录
cd backend

# 安装依赖并编译
mvn clean install -DskipTests

# 启动服务
mvn spring-boot:run
```

### 方式二：使用IDE
1. 用 IntelliJ IDEA 打开 `backend` 目录
2. 等待 Maven 依赖下载完成
3. 运行 `WenChuangShopApplication.java`

### 验证后端启动
访问 http://localhost:8080/api/categories

如果返回类似以下 JSON，则后端启动成功：
```json
{
  "code": 200,
  "message": "操作成功",
  "data": [
    {"id": 1, "name": "手工艺品", "sort": 1, "status": 1},
    ...
  ]
}
```

---

## 启动前端服务

### 1. 安装依赖
```bash
# 进入前端目录
cd frontend

# 安装依赖
npm install
```

### 2. 启动开发服务器
```bash
# 启动开发服务器 (默认端口4200)
npm start
```

### 3. 访问应用
打开浏览器访问: http://localhost:4200

---

## 默认账户

### 管理员账户
- **用户名**: `admin`
- **密码**: `admin123`

### 测试用户
- **用户名**: `user1`
- **密码**: `123456`

---

## 功能验证指南

### 1. 普通用户功能

#### 1.1 注册登录
- [ ] 访问 http://localhost:4200/register 进行注册
- [ ] 使用新账户登录
- [ ] 登录后头部显示用户名

#### 1.2 浏览商品
- [ ] 首页显示热销和最新商品
- [ ] 点击"商品"进入商品列表页
- [ ] 支持按分类筛选商品
- [ ] 支持关键词搜索商品
- [ ] 列表分页显示正常
- [ ] 点击商品进入详情页
- [ ] 商品详情页显示商品信息、用户评价

#### 1.3 热销排行
- [ ] 点击"热销排行"查看销量最高的商品
- [ ] 显示 Top 排名标签

#### 1.4 购物车
- [ ] 在商品详情页选择数量，点击"加入购物车"
- [ ] 点击顶部"购物车"进入购物车页面
- [ ] 支持勾选/全选商品
- [ ] 支持增减商品数量
- [ ] 支持删除商品
- [ ] 勾选商品后自动计算总价
- [ ] 点击"去结算"进入确认订单页

#### 1.5 下单购买
- [ ] 确认订单页填写收货信息（姓名、电话、地址）
- [ ] 显示商品清单和总金额
- [ ] 点击"提交订单"创建订单
- [ ] 跳转到订单详情页
- [ ] 购物车中已下单的商品被移除

#### 1.6 订单管理
- [ ] 点击"我的订单"查看订单列表
- [ ] 支持按订单状态筛选
- [ ] 分页显示正常
- [ ] 点击订单进入详情页
- [ ] 订单详情页显示收货信息、商品清单、订单金额
- [ ] 状态为"待确认"的订单可点击"确认订单"
- [ ] 状态为"已发货"的订单可点击"确认收货"
- [ ] 状态为"待确认"或"已确认"的订单可取消

#### 1.7 用户评价
- [ ] 订单状态为"已完成"时，商品后显示"评价"按钮
- [ ] 点击"评价"弹出评价窗口
- [ ] 可选择1-5星评分
- [ ] 填写评价内容
- [ ] 提交评价成功
- [ ] 评价显示在商品详情页

---

### 2. 管理员功能

#### 2.1 登录管理后台
- [ ] 使用管理员账户登录
- [ ] 顶部导航显示"管理后台"
- [ ] 点击进入管理后台

#### 2.2 商品管理
- [ ] 显示商品列表（ID、图片、名称、价格、库存、销量、状态）
- [ ] 支持关键词搜索商品
- [ ] 分页显示正常

#### 2.3 订单管理
- [ ] 显示订单列表
- [ ] 状态为"已确认"的订单显示"发货"按钮
- [ ] 点击"发货"后订单状态变为"已发货"

#### 2.4 分类管理
- [ ] 显示所有商品分类

---

## API 接口说明

### 认证接口
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 用户登录 |
| POST | /api/auth/register | 用户注册 |
| GET | /api/auth/me | 获取当前用户信息 |

### 商品接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/products | 商品列表（分页） |
| GET | /api/products/hot | 热销商品列表 |
| GET | /api/products/{id} | 商品详情 |
| GET | /api/categories | 分类列表 |

### 购物车接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/carts | 获取购物车列表 |
| POST | /api/carts | 添加到购物车 |
| PUT | /api/carts/{id} | 更新数量 |
| DELETE | /api/carts/{id} | 删除购物车项 |
| DELETE | /api/carts | 清空购物车 |

### 订单接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/orders | 订单列表（分页） |
| GET | /api/orders/{id} | 订单详情 |
| POST | /api/orders | 创建订单 |
| PUT | /api/orders/{id}/pay | 确认订单 |
| PUT | /api/orders/{id}/complete | 确认收货 |
| PUT | /api/orders/{id}/cancel | 取消订单 |

### 评价接口
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/reviews/product/{id} | 商品评价列表 |
| GET | /api/reviews/my | 我的评价 |
| POST | /api/reviews | 创建评价 |
| DELETE | /api/reviews/{id} | 删除评价 |

### 管理员接口 (需 ROLE_ADMIN)
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/admin/products | 商品管理列表 |
| POST | /api/admin/products | 创建商品 |
| PUT | /api/admin/products/{id} | 更新商品 |
| PUT | /api/admin/products/{id}/status | 更新商品状态 |
| PUT | /api/admin/products/{id}/stock | 更新库存 |
| GET | /api/admin/orders | 订单管理列表 |
| PUT | /api/admin/orders/{id}/delivery | 发货 |
| GET | /api/admin/categories | 分类列表 |
| POST | /api/admin/categories | 创建分类 |
| PUT | /api/admin/categories/{id} | 更新分类 |
| DELETE | /api/admin/categories/{id} | 删除分类 |
| GET | /api/admin/users | 用户列表 |
| PUT | /api/admin/users/{id}/status | 更新用户状态 |

---

## 订单状态说明

| 状态值 | 状态名 | 说明 | 可执行操作 |
|--------|--------|------|------------|
| 0 | 待确认 | 订单已创建，等待确认 | 确认订单 / 取消订单 |
| 1 | 已确认 | 用户已确认，等待发货 | 取消订单 |
| 2 | 已发货 | 商家已发货，等待收货 | 确认收货 |
| 3 | 已完成 | 交易完成 | 评价商品 |
| 4 | 已取消 | 订单已取消 | 无 |

---

## 常见问题

### 1. 数据库连接失败
- 检查 MySQL 服务是否启动
- 检查用户名密码是否正确
- 检查数据库 `wenchuang_shop` 是否创建

### 2. 前端无法连接后端
- 确认后端已启动在 8080 端口
- 检查浏览器控制台是否有 CORS 错误
- 后端已配置允许 http://localhost:4200 跨域访问

### 3. 图片显示异常
- 商品图片使用动态生成的 SVG 占位图
- 实际部署时可替换为真实图片地址

### 4. 登录失败
- 确认已执行 `data.sql` 导入初始数据
- 默认密码存储为 BCrypt 加密，无法直接查看
- 管理员: admin / admin123
- 普通用户: user1 / 123456

---

## 生产部署说明

### 后端打包
```bash
cd backend
mvn clean package -DskipTests
# 生成的 jar 包在 target/wenchuang-shop-1.0.0.jar
```

### 前端打包
```bash
cd frontend
npm run build
# 生成的静态文件在 dist/wenchuang-shop
```

### 部署建议
1. 后端使用 Nginx 反向代理
2. 前端静态文件由 Nginx 提供
3. 配置 HTTPS 证书
4. 数据库使用主从复制
5. 添加 Redis 缓存层

---

## 项目特色

1. **温暖简洁的文艺风格UI**
   - 采用暖色调配色（米黄色、棕色）
   - 简洁优雅的界面设计
   - 响应式布局，支持移动端

2. **完整的电商功能**
   - 用户注册登录
   - 商品浏览搜索
   - 购物车管理
   - 下单购买流程
   - 订单管理
   - 用户评价系统
   - 热销排行

3. **完善的管理后台**
   - 商品管理（CRUD、上下架、库存）
   - 订单处理（发货、状态管理）
   - 分类管理

4. **技术亮点**
   - JWT 无状态认证
   - MyBatis-Plus 简化数据库操作
   - 统一的 RESTful API 响应格式
   - 路由守卫保护敏感页面
   - HTTP 拦截器自动注入 Token

---

如有任何问题，请检查日志输出或参考上述说明。
